<div class="flex justify-center mt-2 text-sm text-gray-500">
    <p>{{ __('Showing data from :startDate to :endDate', ['startDate' => $startDate, 'endDate' => $endDate]) }}{{ $contextInfo }}</p>
</div>
